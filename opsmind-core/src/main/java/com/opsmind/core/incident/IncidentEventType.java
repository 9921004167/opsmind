package com.opsmind.core.incident;

/** Timeline entry types actually produced in Phase 1. More are added as later phases add capability. */
public enum IncidentEventType {
    INCIDENT_CREATED,
    STATUS_CHANGED,
    ALERT_LINKED,
    NOTE_ADDED
}
